import { useState } from "react";

export default function NoResultAskAndSendRequestIsbn({
  isbn13,
}: {
  isbn13: number;
}) {
  // request에 있는지 확인하고
  // request에 없으면 요청을 보낼것인지에 작성
  return <div>HI</div>;
}
